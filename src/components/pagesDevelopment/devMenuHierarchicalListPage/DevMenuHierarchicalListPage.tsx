//import { ClassStyleDefinition }                 from "../devMenuListPage/ClassStyleDefinition";
import DevelopmentMasterPageWrapper             from '../devMasterPageWrapper/DevelopmentMasterPageWrapper';
import React                                    from 'react';

const DevMenuHierarchicalListPage: React.FC = () => {
   
//    const classStyles = ClassStyleDefinition();    

    return (
        <DevelopmentMasterPageWrapper 
        title="Menu Hierarchical List"
        description="UI Component present a consistent Hierarchical menu list">            
            </DevelopmentMasterPageWrapper>
    );
}

export default DevMenuHierarchicalListPage;
