import { makeStyles } from '@material-ui/core/styles';

export const ClassStyleDefinition = makeStyles(theme => ({
    paper: {        
        marginTop:16,
        marginBottom:16,
        padding:16

    },

})
);