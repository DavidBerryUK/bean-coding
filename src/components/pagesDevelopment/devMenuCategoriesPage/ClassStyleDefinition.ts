import { makeStyles }                           from '@material-ui/core/styles';


export const ClassStyleDefinition = makeStyles(theme => ({

    menuContainer: {      
        width:300,
        height:400,       
        float:'left',
        margin:8, 
      }
})
);