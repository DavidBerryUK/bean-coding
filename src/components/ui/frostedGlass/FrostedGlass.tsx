
import React                                    from 'react';

const ProductViewer: React.FC = (props) => {
   
    return (
        <div >
            {props.children}
        </div>
    )
}

export default ProductViewer
