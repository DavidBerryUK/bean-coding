import { makeStyles }                           from '@material-ui/core/styles';

export const ClassStyleDefinition = makeStyles(theme => ({

    root: {
        width: 200,
        height: 200,                
        marginBottom:8,        
    },
    media: {
        height: 146,
      },
      cardContent : {
        paddingTop:2
      }

})
);